# Retired identity: `larena/docara-admin`

This archive reference exists only to interpret old repositories and evidence.
It is not a current package. Load [`docara.md`](./docara.md) for all current
implementation, validation, release and ownership decisions.

The old editor/Pro idea survives only as a capability/edition tier inside
`larena/docara`; it must not recreate a split package or repository.
