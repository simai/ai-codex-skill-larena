# Retired identity: `larena/rest_doc`

This archive reference exists only to interpret old repositories, routes and
evidence. `larena/rest_doc` and `larena/rest-doc` are not current packages.

For all current work load [`rest.md`](./rest.md). `larena/rest` owns API
execution, OpenAPI generation, documentation filtering and runtime tests.
Never create a new package, dependency, worktree, release or readiness claim
under either retired identity.
